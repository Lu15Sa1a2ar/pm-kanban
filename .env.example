# Copy to .env at the repo root. Never commit .env.
OPENROUTER_API_KEY=
# Optional: leave empty for the local SQLite file.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Optional limits (defaults shown).
# AI_MESSAGE_LIMIT=10
# AI_DAILY_LIMIT=200
# GUEST_RATE_LIMIT=5
# MAX_BOARD_BYTES=262144
# Production only (set in Vercel): CRON_SECRET, and PRODUCTION=1 outside Vercel.
