# Scripts

This directory contains platform-specific commands for the local Docker deployment.

- `start.ps1` and `stop.ps1`: PowerShell scripts for Windows.
- `start.bat` and `stop.bat`: Command Prompt scripts for Windows.
- `start.sh` and `stop.sh`: shell scripts for macOS and Linux.

All scripts operate from the repository root and delegate lifecycle management to Docker Compose.